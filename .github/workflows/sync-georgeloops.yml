name: Sync GeorgeLoops

on:
  schedule:
    - cron: "0 15 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out Loops Radar
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Sync public GeorgeLoops markdown
        run: npm run sync:georgeloops
        env:
          GEORGELOOPS_BRANCH: master
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify synced app
        run: |
          npm run typecheck
          npm run build

      - name: Open pull request when loops change
        uses: peter-evans/create-pull-request@v7
        with:
          branch: automation/sync-georgeloops
          commit-message: "Sync GeorgeLoops markdown"
          title: "Sync GeorgeLoops markdown"
          body: |
            Weekly sync from georgewangyu/GeorgeLoops.

            This updates Loops Radar so each loop detail page copies the source markdown from the public GeorgeLoops repo.
